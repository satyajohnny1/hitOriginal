# Copy this file to .env and fill in real values. .env is gitignored and
# must be created manually on each environment (local, staging, production).

DB_HOST=localhost
DB_NAME=testdb
DB_USER=testdb
DB_PASS=testdb
